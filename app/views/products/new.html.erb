<% provide(:title, 'New Item') %>
<h1>Add New Item</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@product) do |f| %>
        <%= render 'fields', f: f %>
        <%= f.submit "Create item", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>